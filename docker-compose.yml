version: "3.8"

services:
  db:
    image: postgres:14-alpine
    container_name: flight_system_db
    restart: always
    env_file:
      - ./backend/.env
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
